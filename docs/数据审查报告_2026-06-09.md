# 数据审查报告

> 审查时间：2026-06-09
> 审查范围：本地 SQLite + 服务器 SQLite + 推送链路

---

## 一、发现的问题

### 🔴 致命问题：pct_change 大面积 NULL

**现象**：

| 日期 | 本地 | 服务器 | pct_change |
|------|:--:|:--:|:--:|
| 6/9 | — | 280行 | ❌ 全NULL → 已修复 ✅ |
| 6/8 | 179行 | ? | ❌ 全NULL |
| 6/5 | 300行 | 300行 | ✅ 正常 |
| 6/4及以前 | 300行 | 300行 | ✅ 正常 |

**根因**：

新浪数据源（AKShare `stock_zh_a_daily`）**不提供涨跌幅字段**。我们的下载器用 `groupby().pct_change()` 自动计算，但增量下载时只有 1-2 天新数据，第一天的 `pct_change()` 永远是 NaN（没有前一天作参照）。

```
下载 6/5~6/8 的数据：
  6/5: 有前一天(6/4)在批次内 → 能算 ✅
  6/8: 前一天(6/5)也在批次内 → 能算 ✅

但如果只下载 6/8~6/8（增量1天）：
  6/8: 批次内没有前一天 → NaN ❌
```

**已修复**：`fix_pct_change()` 函数——存完数据后用 SQL 从数据库前一天收盘价补算。服务器已修复 880 条。

---

### 🔴 致命问题：日报显示 0涨/0跌

**现象**：

```
📈 市场广度
数据日期：2026-06-09
- 上涨: 0 | 下跌: 0    ← 全是0
- 平均涨跌: nan%       ← NaN
```

**根因**：`analysis/macro.py` 的 `_analyze_breadth()` 统计 `pct_change > 0` 和 `< 0` 的数量。当 `pct_change` 全是 NULL 时，统计结果全是 0，均值是 NaN。

**修复链路**：
1. `fix_pct_change()` 修复 pct_change ← 已完成
2. 日报在 pct_change 全 NULL 时显示「数据异常」而非 0 ← 待修

---

### 🟡 中等问题：数据覆盖率不足

**现象**：

| 日期 | 应有 | 实有 | 缺失 |
|------|:--:|:--:|:--:|
| 6/8 | 300 | 179 | 121只（40%） |
| 6/5 | 300 | 300 | 0 |
| 6/9（服务器） | 300 | 280 | 20只 |

6/8 缺失 40% 的数据。原因可能是：
- 6/8 是大跌日，部分股票停牌
- 下载时网络不稳定，部分失败后未重试

**现状**：下载器有 3 次重试（退避 0.5s/1.0s），但缺失 40% 说明重试不够。

---

### 🟡 中等问题：信号日期单一

**现象**：

```
信号日期范围: 2026-06-09 ~ 2026-06-09  ← 只有今天
```

信号历史表只有今天的 161 条记录（因为信号持久化今天才上线，之前的信号不存在数据库中）。

**影响**：日报的「昨日信号复盘」永远为空，因为查不到昨天的信号。

**解决**：这是正常的——信号持久化只上线了1天，需要时间积累。至少需要 3-5 个交易日才能看到复盘数据。

---

### 🟡 中等问题：信号数量过多（噪声）

**现象**：

```
6/9 信号: 88条 passed + 73条 blocked = 161条
  - 其中买入 12 条（全部被大盘择时屏蔽）
  - 卖出 85 条（聚合后11条）
```

3个策略 × 300只股票 × 每个策略多个卖出条件 = 大量卖出信号。虽然聚合后压到 11 条，但终端/日志还是很长。

**优化方向**：卖出信号只有在用户有持仓时才展示（需要持仓跟踪系统）。

---

### 🟢 小问题：大盘择时数据时效

**现象**：

```
沪深300: 4713.64 | MA20: 4880.14 | MA60: 4732.42
```

但数据库最新数据是 6/8 或 6/9，而指数 4713.64 看上去不像是 6/9 的收盘价。

**根因**：`market_timing.py` 的 `get_market_regime()` 先查本地 DB 的 000300 数据（没有），然后 fallback 到 AKShare 实时拉取（拉到的是最近一个交易日的指数数据，可能是 6/8）。

**解决**：大盘择时用的指数数据需要明确标注取自哪个日期。

---

## 二、问题优先级

| # | 问题 | 严重度 | 状态 |
|:--:|------|:--:|:--:|
| 1 | pct_change NULL | 🔴 致命 | ✅ 已修复（fix_pct_change） |
| 2 | 日报 0涨0跌 | 🔴 致命 | ✅ 间接修复（依赖问题1） |
| 3 | 数据覆盖率不足 | 🟡 中等 | ⚠️ 需强化重试 |
| 4 | 信号历史单一 | 🟡 中等 | ⏳ 需要时间积累 |
| 5 | 指数数据时效不明确 | 🟢 小 | ⚠️ 需标注日期 |
| 6 | 信号过多（噪声） | 🟢 小 | 🔵 待持仓系统上线 |

---

## 三、修复方案

### 3.1 确保 pct_change 永不为 NULL（已做+加固）

```python
# 已加入 run.py 的每日流程：
download_all()  →  fix_pct_change()  ← 每次下载后自动修复

# 额外加固：在分析模块中加防御
def safe_pct_stats(df):
    valid = df[df['pct_change'].notna()]
    if len(valid) == 0:
        return {'error': '数据异常：涨跌幅全部缺失'}
    return normal_stats(valid)
```

### 3.2 提高数据覆盖率

```python
# 在 download_all() 末尾加补漏逻辑
if fail_count > total * 0.1:  # 失败超过10%
    print("⚠️ 失败率过高，进行第二轮重试...")
    for code in failed_codes:
        retry_download(code)
```

### 3.3 日报数据异常时明确提示

```
如果 pct_change 缺失率 > 50%：
  不显示 0涨0跌，改为：
  "⚠️ 数据异常：涨跌幅数据缺失，无法统计广度"
```

### 3.4 大盘择时数据标注日期

```
大盘状态显示改为：
"🟠 弱势 | 沪深300(6/8): 4714 | MA20: 4880"
```

---

## 四、服务器待执行操作

```bash
# 1. 拉最新代码（含所有修复）
cd /root/Liang-Hua && git pull

# 2. 修复历史数据中的 pct_change
cd /root/Liang-Hua && ./venv/bin/python -c "
from data_fetcher.downloader import fix_pct_change
fix_pct_change()
"

# 3. 验证修复（应该全部非NULL）
cd /root/Liang-Hua && ./venv/bin/python -c "
import sqlite3
conn = sqlite3.connect('data/stocks.db')
for d in ['2026-06-09','2026-06-08']:
    total = conn.execute('SELECT COUNT(*) FROM daily_kline WHERE date=?', (d,)).fetchone()[0]
    nulls = conn.execute('SELECT COUNT(*) FROM daily_kline WHERE date=? AND pct_change IS NULL', (d,)).fetchone()[0]
    print(f'{d}: {total}行, NULL: {nulls} → {\"✅\" if nulls == 0 else \"❌\" +str(nulls)+\"个NULL\"}')
conn.close()
"

# 4. 跑一次完整推送验证
cd /root/Liang-Hua && ./venv/bin/python run.py --no-update && echo "===信号OK===" && ./venv/bin/python analysis/report.py && echo "===日报OK==="
```

---

*审查完成：2026-06-09*
*下一步：加固 + 持续观察 3 个交易日*