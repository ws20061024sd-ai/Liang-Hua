# 优化执行计划

> 基于：[项目综合审查报告_2026-07-05](项目综合审查报告_2026-07-05.md)
> 原则：先修阻断项 → 再修高风险项 → 最后改善项。每步验证后再下一步。

---

## Phase 1：阻断项修复（5 项）

### 1.1 🔴 B1 — 修复 macro.py L164 NameError

- **文件**：`analysis/macro.py`
- **改动**：L164 `str(max_date)` → `str(data_date)`
- **验证**：`python -c "from analysis.macro import _analyze_breadth; print(_analyze_breadth('2020-01-01'))"` 不报错
- **预计**：5 分钟

### 1.2 🔴 B4 — 密钥脱敏

- **步骤**：
  1. 创建 `config/settings.local.py`（加入 `.gitignore`），存放 token
  2. 修改 `config/settings.py` → 从环境变量/ local 文件读取
  3. 确认 `.gitignore` 包含 `settings.local.py`
  4. 提醒用户轮换钉钉 Webhook token 和 Tushare token
- **验证**：`python -c "from config import settings; print(bool(settings.DINGTALK_WEBHOOK))"` 仍能读取到 token
- **预计**：20 分钟

### 1.3 🔴 B2 — 关键路径异常加日志（第一阶段）

- **范围**：5 个最关键位置
  - `run.py:200-203`：板块缓存失败
  - `run.py:189-194`：财务下载失败
  - `market_timing.py:147-152`：策略风格映射失败
  - `sector_trend.py:60`：板块数据保存失败
  - `analysis/macro.py:166`：广度分析异常
- **改动**：每个 `except Exception` 至少打印 `[模块名] 异常: {e}`
- **预计**：20 分钟

### 1.4 🔴 B5 — 数据库连接改为 context manager

- **文件**：`data_fetcher/downloader.py`, `data_fetcher/cleaner.py`, `engine/signal_store.py`
- **改动**：`get_db_connection()` 返回支持 `with` 语句的对象，或直接改用 `sqlite3.connect()` 的 context manager
- **验证**：`python run.py --no-update` 正常运行
- **预计**：40 分钟

### 1.5 🔴 B3 — 创建首批核心测试

- **文件**：新建 `tests/test_risk_filter.py`、`tests/test_signal_aggregator.py`、`tests/test_ma_cross.py`
- **内容**：每个文件 3-5 个关键测试用例
- **验证**：`python -m pytest tests/ -v` 全部通过
- **预计**：1.5 小时

---

## Phase 2：高风险项修复（8 项）

### 2.1 H1 — 引入 logging 模块

- **文件**：`run.py`, `analysis/report.py`（两个入口）
- **改动**：添加 `config/logging_config.py`，入口文件先调用 `setup_logging()`
- **预计**：1 小时

### 2.2 H2 — factor_engine.py 改为批量查询

- **改动**：循环内 300 次 SQL → 1 次批量查询 + pivot
- **预计**：30 分钟

### 2.3 H3 — financial_downloader.py 打印信息修正

- **改动**：L219 "多线程下载" → "顺序下载"
- **预计**：2 分钟

### 2.4 H4 — market_timing.py 风格映射失败加 warning

- **改动**：`except Exception: return {}` 前加 `print(⚠️)`
- **预计**：5 分钟

### 2.5 H5 — run.py 财务下载失败后因子引擎加数据覆盖检查

- **改动**：因子引擎运行前检查 financial_data 覆盖率
- **预计**：20 分钟

### 2.6 H6 — 回测文件参数改为从 settings 读取

- **文件**：`backtest/simple_backtest.py`, `backtest/local_factor_backtest.py`
- **预计**：20 分钟

### 2.7 H7 — 动量突破卖出 strength 动态计算

- **文件**：`strategies/momentum_breakout.py`
- **预计**：15 分钟

### 2.8 H8 — 均值回归 prev_below_lower 启用或删除

- **文件**：`strategies/mean_reversion.py`
- **预计**：10 分钟

---

## Phase 3：中低优先级改善（15 项）

分批次逐步完成，每批 3-5 项，不在此详细列出。详见审查报告 §三。

---

## 执行顺序

```
Phase 1（今天）
  B1 → B4 → B2 → B5 → B3

Phase 2（明天）
  H3 → H4 → H7 → H8 → H6 → H2 → H5 → H1

Phase 3（后续）
  每批 3-5 项，逐步推进
```

---

*计划制定：2026-07-05*
