# 项目下一步计划

> 2026-07-28 | 代码审查修复 + 服务器部署

---

## 🔴 当前：代码审查修复（2026-07-28）

五轴代码审查发现 2 Critical + 2 Important，详见 `docs/核心/findings.md`。

### Critical 修复

| # | 文件 | 问题 | 状态 |
|---|------|------|:----:|
| 1 | `run.py:202` | `settings` 未导入即使用（NameError，ROE 检查静默失败） | ✅ |
| 2 | `scripts/data_check.py:216` | `minr`/`maxr` 未定义（NameError，数据校验崩溃） | ✅ |

### Important 修复

| # | 文件 | 问题 | 状态 |
|---|------|------|:----:|
| 3 | `run.py` + `data_check.py` + `settings.py` | ROE 阈值 255 三处硬编码 → 统一到 settings | ✅ |
| 4 | `engine/runner.py:84` | 策略异常仅 verbose 可见 → 至少 log.warning | ✅ |

### 验证步骤

```bash
python -m pytest tests/ -v          # 26个测试
python scripts/data_check.py         # 数据校验无崩溃
python run.py --no-update            # 信号生成正常
```

---

## 当前阶段

M1 完成，代码已 push 到 GitHub。先做部署让仪表盘上线，后期再逐模块优化。

---

## M1：历史模块 ✅ 已完成

- [X] 真实沪深300指数（AKShare → `index_daily` 表）
- [X] 全市场成交额
- [X] A股红涨绿跌
- [X] market.html 顶部沪深300 SVG K线（MA5/10/20/60 + 成交量）
- [X] history.html 表头直观化 + 图例 + 较昨日列 + 市场方向列
- [X] 大盘择时不再拦截买入信号
- [X] 数据下载优化（批量SQL + 去sleep）
- [X] 代码已 push GitHub（43 commits）

---

## 最优先：服务器部署 + 博客集成 ✅ 已完成（2026-07-28）

服务器已完成部署，5 条 cron 正常运行。`xoln.me/quant` 每工作日 21:20 自动刷新。

### 已完成清单

- [X] 服务器代码更新（手工 heredoc 打补丁——GitHub 从服务器连不上）
- [X] ROE 数据下载（300 只，19295 行）
- [X] 数据库自动迁移（`ensure_schema.py`）
- [X] 成分股本地缓存（不再每次调 API）
- [X] Baostock 增量下载
- [X] COS 上传配置 + coscmd 安装
- [X] 博客程序页（iframe 嵌入 `xoln.me/works/quant`）
- [X] Crontab 5 条定时任务配置
- [X] 修复打全：ROE 校验达标准则、东财警告降噪

### 最终 cron（已生效）

```
0  21 * * 1-5  run.py              → 信号 + 钉钉
5  21 * * 1-5  report.py           → 日报 + 钉钉
10 21 * * 1-5  health_check.py     → 健康检查
15 21 * * 1-5  backup              → DB 备份
20 21 * * 1-5  generate.py + coscmd → 仪表盘更新
```

### 注意事项

- GitHub 从服务器连不上，代码更新需手工 heredoc 或 scp
- COS 密钥已暴露过，已轮换

---

## M2：信号首页（待部署后讨论）

### 当前状态

- geo-blobs hero + 数据审查 banner + 市场条 + 信号排名 + 拦截统计 + stock_*.html 详情页

### 待讨论优化点

| 项         | 说明                     |
| ---------- | ------------------------ |
| 弱信号标注 | 强度 < 0.10 标为"弱信号" |
| 持仓板块   | 有持仓显示，无则隐藏     |
| 空信号建议 | 根据拦截原因给出操作建议 |

---

## M3：市场监控页（待讨论）

| 项           | 说明                            |
| ------------ | ------------------------------- |
| 成交额趋势   | 加近 30 天成交额柱状图          |
| 涨跌家数趋势 | 从 history 快照读取             |
| 板块启用     | report.py 积累 ≥5 天后自动显示 |

---

## M4：策略分析页（待讨论）

| 项         | 说明                        |
| ---------- | --------------------------- |
| 信号统计   | 每个策略显示触发次数/通过率 |
| 硬编码数字 | 已清理                      |

---

## M5：数据审查系统（待讨论）

| 项         | 说明                         |
| ---------- | ---------------------------- |
| 审查项扩展 | 加指数完整性、成交额异常检测 |
| 分级       | 正常/注意/阻断               |

---

## 不改的事

- 基础风控（ST/涨停/停牌/买不起）继续拦截
- 三策略参数不动
- 回测以聚宽为准
