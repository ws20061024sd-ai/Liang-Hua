# 项目下一步计划

> 2026-08-16 | 两轮外部审查闭环 + 仪表盘事故修复完成，待服务器部署

---

## 📌 当前状态（2026-08-16）

系统整体健康，**所有修复已推送 GitHub（本地与远端同步，71 测试全绿），唯一待办：服务器 `git pull` 部署**（周一 21:00 cron 前必须完成）。

| 项 | 状态 |
|---|---|
| 代码 | 最新 commit `54f3940`，本地 = 远端 |
| 测试 | 71 个全过（38 → 71，两轮审查共新增 33 个） |
| 数据 | 8-14（周五）最新，cron 工作日自动更新 |
| 仪表盘 | 线上双路径新数据 + 热力图/tooltip 已验证 ✅ |
| 服务器 | ⏳ 待 `git pull`（最新代码含涨跌停阈值/确认期等） |
| 复盘 | 第十九章（外部审查修复）+ 第十八章（quant.html 事故）已归档 |

### 本周三条主线（均已闭环）

1. **手机端 7 号数据事故**（8-16）：quant.html 死文件 + CDN 规则变更残留 → 修复（generate.py 双入口同源生成 + CDN 刷新 + quant.html 纳入 cron 目录上传）
2. **外部深度审查 ×2**（DeepSeek harness，只读）：20 + 6 条发现，核实后修复 23 个问题，详见复盘第十九章
3. **代码审查补修**：成分股误删保护、覆盖率口径对齐、涨跌停阈值按板块等 10 项

---

## 🔴 待办清单（2026-08-16 更新）

### 立即

- [ ] **服务器 `git pull` + 确认**：`cd /root/Liang-Hua && git pull`，周一 21:00 前必须
- [ ] **周一首跑观察**：`logs/cron.log` + `logs/health.log` + 钉钉——确认涨跌停新阈值、止损确认期、health.html 覆盖率真实值
- [ ] **仪表盘更新验证**：周一 21:20 后手机 + 电脑各看一次（双路径数据一致）

### 中优先级

| 事项 | 说明 | 来源 |
|---|---|---|
| ST 标记数据源 | 接 AKShare ST 板块接口写入 `stock_info.is_st`；目前 ST 过滤空转（沪深300 极少 ST，影响有限） | 审查问题3 |
| 持仓确认入口 | 半自动无用户反馈通道——已用"3 天确认期 + 诚实标注"缓解，长期需确认/忽略入口 | 审查问题2 |
| qfq 除权缺口 | 前复权增量下载在除权除息后历史价口径不一致 → 定期全量刷新或标注局限 | 审查问题15 |

### 低优先级（已核实影响有限）

| 事项 | 说明 |
|---|---|
| 非交易日先下载后判断 | `run.py` 顺序问题，实际仅浪费几分钟下载耗时 |
| WAL 备份方式 | `shutil.copy2` → 建议换 `sqlite3.Connection.backup()`（连接全关时自动 checkpoint，实际风险低） |
| ensure_schema 不建表 | 表不存在时只加列不 CREATE；各模块自带建表，实际无碍 |
| get_recent_signals 时区 | `date('now')` 是 UTC，当前无生产调用方，未来接入准确率统计需改传本地日期 |
| proxy_cleanup 全局副作用 | import 时删除全部代理环境变量 |
| "全市场成交额"是近似值 | 实为沪深300成分股 SUM(amount)，非全市场 |

### 里程碑遗留（M2-M5 待讨论，已冻结）

M2 信号首页 / M3 市场监控页 / M4 策略分析页 / M5 数据审查系统——见下方里程碑节，优先级低于当前待办。

---

## ✅ 近期已完成（2026-08 回看）

### 8-16：quant.html 死文件事故（复盘第十八章）

- 手机端永远 7 号数据根因：8-9 手动上传的 `quant.html` 从未被 cron 更新 + CDN 规则变更残留 30 天旧缓存
- 修复：`generate.py` 双入口同源生成 + CDN 刷新 5 URL + cron 目录上传自动覆盖
- 决策 35-37 + 代码模式 5.20-5.21（多入口验证/死文件审计）

### 8-16：外部审查批量修复（复盘第十九章，10 commit）

| 修复 | 测试 |
|---|---|
| SELL 只拦跌停/停牌（不套买入限制） | +6 |
| 移动止损 SELL 3 天确认期 | +4 |
| 成分股 30 天自动刷新 + 调出股删除 + 数量≥250 保护 | +8 |
| PE/PB 覆盖率分母 = 股票池总数（data_check + health.html 对齐），降 warning | +3 |
| 大盘择时优先本地 index_daily + 3 天新鲜度 | +2 |
| 指数成交额回填只处理缺失行 | +2 |
| 无信号占位记录（health_check 不误报） | +2 |
| 涨跌停阈值按板块（双创 20%/ST 5%/主板 10%） | +5 |
| 依赖/except/cron/CLAUDE.md/setup.sh 对齐 | — |

### 8-09：仪表盘优化（已上线验证）

- 板块热力图（色块大小=成分股数）+ K线悬停/点击 tooltip
- 股票详情页改为信号历史全量生成（修残留旧文件 bug）

### 8-02~09：自动更新链路排查（复盘第十七章）

- 上传路径/博客 iframe/CDN 三处错位 → 双路径上传 + 博客代码更新 + max-age=300

---

## 里程碑

### M1：历史模块 ✅ 已完成

真实沪深300指数 / 全市场成交额 / A股红涨绿跌 / K线图表 / 大盘择时不拦截 / 数据下载优化

### M2：信号首页（待讨论）

- 弱信号标注（强度 < 0.10）
- 持仓板块（有持仓显示，无则隐藏）
- 空信号建议（根据拦截原因给出操作建议）

### M3：市场监控页（待讨论）

- 成交额趋势（近 30 天柱状图）
- 涨跌家数趋势（从 history 快照读取）
- 板块启用（report.py 积累 ≥5 天后自动显示）

### M4：策略分析页（待讨论）

- 信号统计（每策略触发次数/通过率）

### M5：数据审查系统（待讨论）

- 审查项扩展（指数完整性、成交额异常检测）
- 分级（正常/注意/阻断）

---

## 不改的事

- 三策略参数不动（MA20/60、10日突破、布林带 10,2.0）
- 回测以聚宽为准，本地仅方向验证
- 大盘择时降权不拦截（v3 决策）
- 仪表盘零 JS 依赖架构
