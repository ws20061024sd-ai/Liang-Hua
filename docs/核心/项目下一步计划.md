# 项目下一步计划

> 2026-07-11 | 逐模块讨论→优化，当前进度：M1 已完成

---

## 当前阶段

逐模块优化。每轮流程：**只讨论 → 确认方案 → 动手改 → 验证提交**。

---

## M1：历史模块 ✅ 已完成

- [x] 真实沪深300指数（AKShare → `index_daily` 表）
- [x] 全市场成交额（`SUM(amount)`）
- [x] A股红涨绿跌（CSS `--up`/`--down` 对调）
- [x] market.html 顶部沪深300 SVG K 线图
- [x] history.html 表头直观化 + 图例说明
- [x] 市场脉搏表加"较昨日"列
- [x] 信号摘要表加"市场方向"列
- [x] 大盘择时不再拦截买入信号
- [x] K 线图 MA 断线修复 + 成交量柱 + MA5/MA10
- [x] 信号首页 0→7 买入通过（大盘择时取消后）

---

## M2：信号首页（待讨论）

### 当前状态
- geo-blobs hero + 数据审查 banner + 市场条（指数+涨跌家数+成交额）
- 买入信号排名（7 只）+ 拦截统计（14 条）
- 7 个 stock_*.html 详情页，每页有 SVG K 线 + 技术指标 + 历史信号

### 待讨论优化点

| 项 | 说明 |
|----|------|
| 弱信号标注 | 强度 < 0.10 的信号标为"弱信号"，提示审慎 |
| 持仓板块 | `position_tracker` 推断持仓，有则显示，无则隐藏整块 |
| 空信号建议 | 拦截原因分类给出具体操作建议（不只是固定文字） |
| 底部链接整理 | health.html 无导航入口，要么加入要么去掉 |

---

## M3：市场监控页（待讨论）

### 当前状态
- 沪深300 SVG K 线（120天，MA5/10/20/60 + 成交量）
- 市场宽度面板：涨跌家数 + 净涨 + 涨跌比 + 涨跌停
- 板块分析：数据积累中（0/5天）

### 待讨论优化点

| 项 | 说明 |
|----|------|
| 成交额趋势 | 加近 30 天成交额柱状图 |
| 30天涨跌家数趋势 | 从 history 快照读取，可视化市场宽度变化 |
| 板块启用 | report.py 积累 ≥5 天后自动显示 |

---

## M4：策略分析页（待讨论）

### 当前状态
- 三张策略卡片：原理、适用环境、参数选择理由
- 硬编码回测数字已移除
- 最近信号展示

### 待讨论优化点

| 项 | 说明 |
|----|------|
| 信号统计 | 每个策略显示近期触发次数/通过率/最近一次日期 |
| 免责声明 | 已完善，保持不变 |

---

## M5：数据审查系统（待讨论）

### 当前状态
- 首页 banner 自动检测 5 项（新鲜度/股票数/NULL/PE/ROE）
- 红色脉冲动画报警

### 待讨论优化点

| 项 | 说明 |
|----|------|
| 审查项扩展 | 加指数完整性、成交额异常检测 |
| 分级 | 🟢正常 / 🟡注意 / 🔴阻断 |
| 历史记录 | 审查结果写入快照 JSON |

---

## 基础设施任务（待执行）

### A. 数据下载优化

| 项 | 说明 |
|----|------|
| 增量下载 | `downloader.py` 加 `--incremental`，查各股票最新日期，只补缺失的几天 |
| 全量校验 | 周末自动全量覆盖 + `data_check.py --block` |
| 改动范围 | 只改 `data_fetcher/downloader.py` + `run.py` |

### B. 服务器推送

| 项 | 说明 |
|----|------|
| git push | 本地 26 个 commit 推到 GitHub |
| 服务器 git pull | 大盘择时取消拦截生效、仪表盘生成器部署 |
| cron 确认 | 加入 `web/generate.py` |

### C. 博客集成

| 项 | 说明 |
|----|------|
| 符号链接 | `ln -s /root/Liang-Hua/web/output /var/www/blog/public/quant` |
| 访问路径 | `/works/quant` → 仪表盘首页，无需后端 |

---

## 不改的事

- 基础风控（ST/涨停/停牌/买不起）继续拦截
- 多因子策略保持辅助参考地位
- 三策略参数（MA20/60, DK10, BB10/2.0）不动
- 回测不本地迭代（以聚宽为准）

---

*下一步：从 M2 开始讨论，或先做基础设施任务。*
